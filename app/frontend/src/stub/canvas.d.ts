interface CanvasRenderingContext2D {
  // The `reset` method exists on `CanvasRenderingContext2D`. Not sure why it is not in the official TypeScript definition.
  // https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/reset
  reset();
}
